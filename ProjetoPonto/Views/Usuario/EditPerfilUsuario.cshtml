@model ProjetoPonto.Entity.PerfilUsuario

@{
    ViewBag.Title = "EditPerfilUsuario";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {

     <div class="form-horizontal">
        <h4>Empresa</h4>
        <hr />

        @Html.HiddenFor(model => model.IdPerfilUsuario)
         @Html.HiddenFor(model => model.IdUsuario)

       <div class="form-group">
            @Html.LabelFor(model => model.DataAutorizacao, new { @class = "control-label col-md-2" })
            <div class="col-md-3">
                @Html.TextBoxFor(model => model.DataAutorizacao, new { @class = "form-control" })
            </div>
        </div>
         

        <div class="form-group">
            @Html.LabelFor(model => model.IdPerfil, new { @class = "control-label col-md-2" })
          <div class="col-md-10">
            @Html.DropDownList("IdPerfil")
        </div>
             </div>

        
         <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Save" class="btn btn-default"  onclick="return validar();" />
          </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "ListaPerfilUsuario", new { idUsuario = Model.IdUsuario })
</div>

@if (ViewBag.Erro != null && ViewBag.Erro != "")
{
    <script>
        alert('@Html.Raw(ViewBag.Erro)');
    </script>
}
<script>
    function validar() {


        return true;
    }
  
</script>